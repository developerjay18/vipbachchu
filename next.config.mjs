/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['pexels.com', 'res.cloudinary.com', 'images.unsplash.com', 'm.media-amazon.com', 'upload.wikimedia.org','wikimedia.org' ],
  },
}

export default nextConfig
