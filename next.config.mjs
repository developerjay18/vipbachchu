/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['pexels.com', 'res.cloudinary.com', 'images.unsplash.com', 'example.com', 'm.media-amazon.com'],
  },
}

export default nextConfig
